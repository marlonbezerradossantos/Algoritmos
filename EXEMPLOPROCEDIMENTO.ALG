Algoritmo "exemploProcedimento"

Var
  x,y:inteiro
procedimento soma(var a, b:inteiro)
inicio
   a <- a + 1
   b <- b + 2
   escreval("Valor de A = ", a)
   escreval("Valor de B = ", b)
   escreval("Soma a + b = ", a+b)
fimprocedimento
Inicio
   x <- 4
   y <- 8
   soma(x,y)
   escreval("Valor de Y = ", x)
   escreval("valor de y = ", y)
Fimalgoritmo